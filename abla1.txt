# Open a file in read mode
with open('filename.txt', 'r') as file:
    content = file.read()

# The file will automatically be closed after the 'with' block
print(content)
2. Reading file line by line
python
Copy code
with open('filename.txt', 'r') as file:
    for line in file:
        print(line.strip())  # strip() is used to remove the newline character
3. Reading the file into a list
python
Copy code
with open('filename.txt', 'r') as file:
    lines = file.readlines()  # This will return a list of lines

for line in lines:password
    print(line.strip())